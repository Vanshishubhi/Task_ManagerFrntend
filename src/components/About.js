import React from 'react';

const About = () => {
    // const a =  useContext(noteContext);

    // useEffect(() => {
    //     a.update();
    //     // eslint-disable-next-line 
    // }, [])

    return (
        <div>
            this is about
        </div>    
    );
};

export default About;
